> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Clean up CSS and migrate custom category badges to use native FluentBadge

**Current behavior:**
Custom badge styling for categories (`bug`, `feature`, `chore`) is manually implemented via custom classes (`.badge-bug`, `.badge-feature`, `.badge-chore`) with hardcoded hex/rgba color codes in both `Board.razor.css` and `Categories.razor.css`. This increases redundant custom CSS and bypasses Fluent's adaptive theme system for light/dark modes.

**Desired behavior:**
1. Replace the custom HTML `<span>` tags representing card categories in `Board.razor` and `Categories.razor` with the native `<FluentBadge>` component.
2. Configure `<FluentBadge>` with `Appearance="BadgeAppearance.Tint"` and map the category names to official `BadgeColor` values:
   - `bug` -> `BadgeColor.Danger`
   - `feature` -> `BadgeColor.Brand`
   - `chore` -> `BadgeColor.Warning`
   - other/unknown -> `BadgeColor.Subtle`
3. Delete all redundant category-specific badge classes (`.category-badge`, `.badge-bug`, `.badge-feature`, `.badge-chore`, etc.) from both `Board.razor.css` and `Categories.razor.css`.
4. Ensure all other custom containers and borders rely on Fluent UI CSS variables (like `var(--neutral-stroke-rest)`) rather than hardcoded hex color values.

**Key interfaces:**
- `Board.razor` and `Categories.razor` components
- `Board.razor.css` and `Categories.razor.css` stylesheets

**Acceptance criteria:**
- [ ] Category badges on the board and categories pages use the native `<FluentBadge>` component.
- [ ] The badge colors map to official Fluent UI `BadgeColor` values and adapt natively to light/dark themes.
- [ ] Hardcoded hex/rgba color styles for categories are completely removed from `Board.razor.css` and `Categories.razor.css`.
- [ ] The application builds and runs successfully, with category pages functioning properly.

**Out of scope:**
- Modifying category CRUD logic.
- Styling other board components beyond standard Fluent UI alignment.
